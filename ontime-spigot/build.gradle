plugins {
    id 'com.github.johnrengelman.shadow' version '5.2.0'
}

jar.enabled = false // we only want shadowJar

repositories {
    maven { url "https://hub.spigotmc.org/nexus/content/repositories/snapshots" }
}

dependencies {
    implementation project(':ontime-api')
    implementation project(':ontime-common')
    implementation project(':ontime-luckperms')
    implementation configurate_hocon
    implementation spigot
}

shadowJar {
    String jarName = "OnTime-Spigot-${project.version}.jar"
    println "Building: " + jarName
    archiveFileName = jarName

    dependencies {
        include project(':ontime-api')
        include project(':ontime-common')
        include project(':ontime-luckperms')
    }
}

artifacts {
    archives shadowJar
}
